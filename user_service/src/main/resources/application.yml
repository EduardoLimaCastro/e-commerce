spring:
  application:
    name: user_service

  datasource:
    url: jdbc:postgresql://localhost:5433/user_service
    username: postgres
    password: password

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: false

  flyway:
    enabled: true
    locations: classpath:db/migration

server:
  port: 8081